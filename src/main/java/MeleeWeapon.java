public class MeleeWeapon extends Weapon {

    public MeleeWeapon(String name, String description, boolean isLocked, int lockID, int damage) {
        super(name, description, isLocked, lockID, damage);
    }
}
